'use strict';

// import { from } from "rxjs";
// let { from } = rxjs;
// let { tap, map } = rxjs.operators;

const utils = require('../src/apiUtils');

const rxjs = require('../libs/rxjs.umd.min.js');

describe('apiUtils.test.js ', () => {
  test('easy test', () => {
    expect(1).toBe(1);
  });
});
